# AC control
msmart-ng              # Midea LAN protocol (Tadiran/Electra via MSmartHome)
httpx                  # kept for potential future use
requests               # Sensibo REST calls (avoids asyncio SSL issues on Windows)

# Speech
faster-whisper
silero-vad
sounddevice
numpy
edge-tts

# Config / utils
pyyaml
python-dotenv
aiohttp
